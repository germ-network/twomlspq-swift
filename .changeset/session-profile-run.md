---
"@germ-network/twomlspq-swift": minor
---

C1 (the §A.5 `Upd′`'s authenticated data) and C2 (deferring the reciprocal §A.5) now follow the session profile actually recorded in the group, instead of always running deployed-compatible. A session with no recorded profile is unaffected and stays deployed-compatible.
