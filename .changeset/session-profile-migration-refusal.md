---
"@germ-network/twomlspq-swift": patch
---

The migration mint now refuses to mint a session around any of the four restored groups recording a session profile: the deployed engine never advertises, so it never records one either.
