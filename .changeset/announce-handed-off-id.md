---
"@germ-network/twomlspq-swift": minor
---

An id-changing §A.5 `Upd′` now carries the handed-off id in its authenticated data (C1), matching the deployed Rust engine's own trigger. Every session runs the deployed-compatible profile until profiles exist. A key-only `Upd′` still carries none. Receive already cross-checked a present value against the proposed leaf's id; that is unchanged.
